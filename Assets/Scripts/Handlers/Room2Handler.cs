using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Room2Handler : MonoBehaviour
{

    public GameObject ceiling;
    // Start is called before the first frame update
    void Start()
    {
        ceiling.SetActive(true);
    }
}
